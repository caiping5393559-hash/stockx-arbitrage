services:
  stockx-goat-app:
    build: .
    container_name: stockx-goat-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
